#include "PNCATEHO.h"

const uint16_t PROGMEM keymaps[][MATRIX_ROWS][MATRIX_COLS] = {

    [0] = LAYOUT(KC_B, KC_F, KC_T, KC_J,
                 KC_H, KC_C, KC_N, KC_Y,
                                   KC_X, KC_S),
};
